import 'package:flutter/material.dart';

class ChangeCharacterPage extends StatelessWidget {
  const ChangeCharacterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Center(child: Text('Change Character')));
  }
}
