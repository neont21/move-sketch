import 'package:flutter/material.dart';

class SessionTrackingPage extends StatelessWidget {
  const SessionTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Center(child: Text('Session Tracking')));
  }
}
