import 'package:flutter/material.dart';

class OSSLicensePage extends StatelessWidget {
  const OSSLicensePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Center(child: Text('License')));
  }
}
